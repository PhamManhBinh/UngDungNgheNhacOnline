<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.musicapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicApp">
        <activity android:name=".Activity.RegisterActivity"></activity>
        <activity android:name=".Activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.TimKiemActivity" />
        <activity android:name=".Activity.PlaylistChuDeActivity" />
        <activity android:name=".Activity.DanhSachBaiHatActivity" />
        <activity android:name=".Activity.DanhSachBaiHatAlbumActivity" />
        <activity android:name=".Activity.AlbumMoiActivity" />
        <activity android:name=".Activity.MusicPlayer" />
        <activity android:name=".Activity.NgheGiHomNayActivity" />
        <activity android:name=".Activity.MainActivity"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>